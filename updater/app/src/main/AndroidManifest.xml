<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Explicit package visibility (API 30+) so getPackageInfo() succeeds for these apps
         without needing the QUERY_ALL_PACKAGES permission. The <package> entries between
         the GENERATED markers are regenerated from ../apps.json by the syncQueries Gradle
         task on every build — do not edit them by hand. -->
    <queries>
        <!-- GENERATED:queries:BEGIN (from apps.json — do not edit by hand) -->
        <package android:name="com.example.industria" />
        <package android:name="dev.jim.acetone" />
        <package android:name="dev.jim.anetmon" />
        <package android:name="dev.jim.brine" />
        <package android:name="dev.jim.didact" />
        <package android:name="dev.jim.sentry" />
        <package android:name="dev.jim.statbot.indicium" />
        <package android:name="dev.jim.tutor" />
        <package android:name="dev.jim.updater" />
        <!-- GENERATED:queries:END -->
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Updater">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="dev.jim.updater.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
